# Frontend build-time env (Next inlines NEXT_PUBLIC_* into the static export).
# Copy to .env.production and point at the live Cloudflare Worker before building.
NEXT_PUBLIC_MCP_URL=https://unified-memory-mcp.rapid-king-4a64.workers.dev
